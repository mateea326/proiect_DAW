@model DevCollab.Models.Answer

@{
    ViewBag.Title = "Edit";
}

<h2 id="h-subjects">Editare raspuns</h2>
<br />

@if (Model != null)
{
    @using (Html.BeginForm(controllerName: "Answers", actionName: "Edit",
                        method: FormMethod.Post, routeValues: new { id = Model.Id }))

    {
        @Html.Label("Content", "Conținut răspuns")

        <br />

        @Html.Editor("Content", new { @class = "form-control" })
        <br />

        @Html.ValidationMessage("Content", null, new { @class = "text-danger" })

        <br />
        <br />

        <button id="buton2" class="btn btn-secondary" type="submit">Editeaza răspunsul</button>

    }
}
else
{
    <p>The model is null</p>
}
